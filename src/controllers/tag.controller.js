export const createTag = async (res, req) => {
  try {
  } catch (error) {
    res.status(500).json({ message: "Error interno del servidor." });
  }
};

export const getAllTag = async (res, req) => {
  try {
  } catch (error) {
    res.status(500).json({ message: "Error interno del servidor." });
  }
};

export const getTagById = async (res, req) => {
  try {
  } catch (error) {
    res.status(500).json({ message: "Error interno del servidor." });
  }
};

export const updateTag = async (res, req) => {
  try {
  } catch (error) {
    res.status(500).json({ message: "Error interno del servidor." });
  }
};

export const deleteTag = async (res, req) => {
  try {
  } catch (error) {
    res.status(500).json({ message: "Error interno del servidor." });
  }
};
